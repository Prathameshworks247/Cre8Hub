# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Atlas Configuration
# Replace the line below with your MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.xxxxx.mongodb.net/cre8hub?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-this-in-production

# Frontend Configuration (for CORS)
FRONTEND_URL=http://localhost:3000

# Optional: Email Configuration (for future features)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: File Upload Configuration (for future features)
# MAX_FILE_SIZE=10485760
# UPLOAD_PATH=./uploads
