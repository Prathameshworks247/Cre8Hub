# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/cre8hub

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Frontend Configuration (for CORS)
FRONTEND_URL=http://localhost:8080

# Optional: MongoDB Atlas (Cloud MongoDB)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/cre8hub?retryWrites=true&w=majority

# Optional: Email Configuration (for future features)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: File Upload Configuration (for future features)
# MAX_FILE_SIZE=10485760
# UPLOAD_PATH=./uploads

# YouTube API Configuration
YOUTUBE_API_KEY=your-youtube-api-key

# Google OAuth Configuration (for YouTube transcript extraction)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8080/auth/youtube/callback

# Redis Configuration (for caching transcripts)
REDIS_URL=redis://localhost:6379

# FastAPI Configuration (for AI persona extraction)
FASTAPI_BASE_URL=http://localhost:8000 